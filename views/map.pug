extend layout

block content
  style.
    #map{
      width:100%;
      height:700px;
      background-color:red;
    }
  body
    div(id="map")
  script.
            function createMap(){
              var mapOptions = {
                zoom: 10,
                mapTypeId: google.maps.MapTypeId.HYBRID,
                center: new google.maps.LatLng(50,50)
              } 
              var map = new google.maps.Map(document.getElementById("map"), mapOptions);
            }  
            window.onload=createMap;
  script(aync defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDJZ5scfMufOInUJdWMkp3Hr6pyxCDUU_M&callback=myMap" type="text/javascript")
